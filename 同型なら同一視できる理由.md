なぜ [[同型]] なら同一視ができるか？を理解するために以下のような状況を考えてみる。

[[圏]] : 世界の言語
[[対象]]$A$ : 日本語
対象$B$ : 日本語と全く同じだけど発音だけ違う言語

この場合に、両者に翻訳という [[射]] が存在した際、両方の言葉の文法や単語は同じなので、お互い全く同じ意味を伝え合うことが可能であるはずだ。この様な場合を同型というのだが、これが何故同一視出来るかというと、他言語（英語）などから対象$A$か$B$に翻訳（射である）した際には、その対象$A,B$同士で翻訳し合うことで双方が全く同じ様に理解することが可能だからである。

また対象$A, B \in \mathscr{A}$が同型な場合、対象$x \in \mathscr{A}$があるとして$Hom_\mathscr{A}(x, A)$と$Hom_\mathscr{A}(x,B)$の数が同じになる。
$$
A, B \in ob(\mathscr{A}), A \cong B \Rightarrow \forall x \in ob(\mathscr{A}), Hom_\mathscr{A}(x, A) \cong Hom_\mathscr{A}(x, B)
$$
つまり、対象$A,B$に来る射の数が同じであるということである。（濃度が同じ）

#同型