[[対象]] 同士を繋ぐもの。
[[始域]]$A$から[[終域]]$B$への射$f$を$f : A \rightarrow B$と表す。

また、始域$A$から終域$B$への射と、始域$B$から終域$C$への射があるなら、それを合成した始域$A$から終域$C$への射が必ず1つ存在する。
$$
\forall a,b,c \in ob(\mathscr{A}), \forall f : a \rightarrow b, \forall g: b \rightarrow c \Rightarrow \exists! g \circ f: a \rightarrow c
$$

例：整数値同士の足し算、犬同士の喧嘩、平仮名の「あ」から「あ」への繋がり、等

#用語 #射