- 圏 (category) : $\mathscr{A}$
- 対象 (object) : $ob(\mathscr{A})$
- 圏$\mathscr{A}$の$A$から$B$への射 (morphism) : $\mathscr{A}(A,B)$
- 対象$A$の恒等射 (identity) : $1_{A}$
- 射$f$の逆射 (inverse) : $f^{-1}$
- 対象$A$と$B$が同型 (isomorphic) : $A \cong B$

しばしば以下の記法も採用している。

- $A \in ob(\mathscr{A})$の意味で$A \in \mathscr{A}$
- $f \in \mathscr{A}(A,B)$の意味で$f : A \rightarrow B$ または $A \xrightarrow{f} B$
- $g \circ f$の意味で$gf$
- $1_{A}$は$id_{A}$とも書かれる
- $\mathscr{A}(A,B)$は$Hom_{\mathscr{A}}(A,B)$あるいは$Hom(A,B)$とも書かれる