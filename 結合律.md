$h \circ g \circ f$の場合に、$(h \circ g) \circ f$と$h \circ (g \circ f)$が成り立つときに、結合律が成り立つという。
これを言い換えると実は、$dom(f)$から$cod(h)$への [[射]] の結果は一意であるということ。

射の合成を以下のように考えることができる
```haskell
 f :: a -> b
 g :: b -> c
 h :: g . f
   :: (a -> b) -> (b -> c)
   :: a -> b -> c
   :: (a -> b) -> c
   :: a -> (b -> c)
```

#用語 #結合律